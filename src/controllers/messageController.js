const Message=require('../models/Message');const Event=require('../models/Event');const AppError=require('../utils/AppError');
exports.create=async(req,res)=>{if(!await Event.exists({_id:req.params.eventId}))throw new AppError('Event not found',404);const msg=await(await Message.create({event:req.params.eventId,sender:req.user.id,text:req.body.text})).populate('sender','name role');req.app.get('io')?.to(`event:${req.params.eventId}`).emit('announcement',msg);res.status(201).json({success:true,data:msg})};
exports.list=async(req,res)=>res.json({success:true,data:await Message.find({event:req.params.eventId}).populate('sender','name role').sort({createdAt:1})});
